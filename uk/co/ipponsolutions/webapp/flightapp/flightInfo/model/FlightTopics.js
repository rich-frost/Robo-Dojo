define([
    "dojo/_base/declare"
],
    function(declare){

        return declare(null, {
			GET_FLIGHT_DATA: "flightapp/flight/get/data",
			SHOW_FLIGHT_INFO: "flightapp/flight/show/info"
		});
        
	}
);